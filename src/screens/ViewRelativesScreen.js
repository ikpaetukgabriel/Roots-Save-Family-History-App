import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ViewRelativesScreen = () => {
	return (
		<View>
			<Text>View Relatives</Text>
		</View>
	);
};

export default ViewRelativesScreen;

const styles = StyleSheet.create({});
